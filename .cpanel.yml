---
deployment:
  tasks:
    - export DEPLOYPATH=/home/vaqttcihl79p/public_html
    - cd vite-project && /usr/bin/npm install && /usr/bin/npm run build
    - /bin/cp -R vite-project/dist/* $DEPLOYPATH
